apiVersion: v1
kind: Secret
metadata:
  name: storefront-backend-secrets

data:
  # Environment variables (base64 encoded)
  API_KEY: ${env.DATABASE_URL}
  DATABASE_PASSWORD: ${env.JWT_SECRET}
  PORT: ${env.PORT}

stringData:
  # Non-sensitive data directly in the secret
  ENVIRONMENT: "development"
