apiVersion: v1
kind: Service
metadata:
  name: storefront-frontend
  labels:
    app: storefront-frontend
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: storefront-frontend
