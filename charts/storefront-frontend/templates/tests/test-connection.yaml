apiVersion: v1
kind: Pod
metadata:
  name: storefront-frontend
  labels:
    app: storefront-frontend
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ["wget"]
      args: ["storefront-frontend:{{ .Values.containerPort }}"]
  restartPolicy: Never
